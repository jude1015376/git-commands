clone clones a repo
checkout changes branch to checkout branch (thats what i use if for)
-b creates new branch and switchs to it
status
push pushes you code to github
-u pushes to orgin branch
pull pulls code from github
add adds new files to commit
commit commits changes so they are stagged for pull
-m allows you to add a meesage to the commit
-a auto maticly stages and tracks files
-am does both -m and -a
branch shows which branch you are on
-a shows all branches open and closed
-d will merge if branch is safe to merge then delete the branch
merge merges branches
